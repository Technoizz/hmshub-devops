apiVersion: apps/v1
kind: Service
metadata:
  name: hms-backend
spec:
  type: NodePort
  selector:
    app: hms-backend
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
      nodePort: 30050