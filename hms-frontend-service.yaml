apiVersion: v1
kind: Service 
metadata:
  name: hms-frontend
  namespace: staging
spec:
  type: NodePort
  selector:
    app: hms-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5001
      NodePort: 30001  
